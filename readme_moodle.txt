Description of third party libraries utilised in local_preview

===============
Guzzle
===============

Guzzle has been installed already via Composer, so you shouldn't have to do anything
For developers - if you wish to update it:
# Download and install composer from [getcomposer.org]
# Run `php composer.phar update` in root of plugin
# Submit PR for plugin so we can all enjoy.
